import { Link } from 'gatsby';
import React from 'react';

function Footer() {
        return (
                <footer>
                        <h4>A footer</h4>
                </footer>
        );
}

export default Footer;
